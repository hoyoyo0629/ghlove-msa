apiVersion: v1
kind: Secret
metadata:
  name: postgres-donation-secret
  namespace: ghlove
type: Opaque
stringData:
  POSTGRES_USER: donation
  POSTGRES_PASSWORD: ghlove
  POSTGRES_DB: donation
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-donation-pvc
  namespace: ghlove
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres-donation
  namespace: ghlove
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres-donation
  template:
    metadata:
      labels:
        app: postgres-donation
    spec:
      containers:
        - name: postgres
          image: postgres:17
          ports:
            - containerPort: 5432
          envFrom:
            - secretRef:
                name: postgres-donation-secret
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data
              subPath: pgdata
          readinessProbe:
            exec:
              command: ["pg_isready", "-U", "donation"]
            initialDelaySeconds: 5
            periodSeconds: 5
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: postgres-donation-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: postgres-donation
  namespace: ghlove
spec:
  selector:
    app: postgres-donation
  ports:
    - port: 5432
      targetPort: 5432
